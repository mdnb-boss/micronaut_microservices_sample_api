micronaut:
  application:
    name: lojaService
  server:
    port: 8081

kafka:
  bootstrap:
    servers: localhost:9092

consul:
  client:
    registration:
      enabled: true
    default-zone: "localhost:8500"